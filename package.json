{
  "name": "mailto",
  "version": "1.0.0",
  "description": "replaces obscured elememt with a mailto link",
  "main": "index.js",
  "author": "Mat Brady <matbr8dy@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "microbundle --output dist --compress false && microbundle --output dist/compressed --compress false",
    "dev": "microbundle watch --output dist --compress false",
    "docs": "jsdoc index.js -t node_modules/minami --readme README.md --destination docs"
  },
  "devDependencies": {
    "jsdoc": "^3.5.5",
    "microbundle": "^0.2.4",
    "minami": "^1.2.3"
  }
}
